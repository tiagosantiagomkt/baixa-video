# 📥 API de Download de Vídeos via Link (FastAPI + yt-dlp)

Cole o link de um vídeo (ex: YouTube) e baixe via API.

## Como rodar localmente

```bash
pip install -r requirements.txt
uvicorn main:app --reload
